# Completed synthetic demonstration outputs

**Demonstration run ID:** `20260901T081036940718Z-ea9100a654cb`

This is a selected, reviewable output package from the completed synthetic
demonstration run. It is not a live deal run and contains no real deal-lead
responses.

## TEST-ONLY SYNTHETIC ANSWERS

`intake/TEST_ONLY_SYNTHETIC_ANSWERS_round_1.json` and
`intake/TEST_ONLY_SYNTHETIC_ANSWERS_round_2.json` contain explicit
**TEST-ONLY SYNTHETIC ANSWERS** used solely to exercise the two intake gates.
They are fictional operator fixtures, are not attributable to Gavin or any
other real person, and must not be reused as live answers.

## What is included

The package preserves the selected run's source register, extraction summary,
both question packets, both labelled synthetic-answer artifacts, final
unresolved-information register, evidence/contradiction/calculation/citation
ledgers, all six analytical outputs, report bundle, task/research ledgers and
validation results. Source IDs and native locators are retained so the report
and ledgers remain cross-referenced.

## What is deliberately excluded

No cache, rendered-page image, temporary file, checkpoint, arbitrary run-state
file, source-document text dump, confidential-room data, or unredacted employee
content is included. The selected run contains no independent red-team challenge
or resolution artifact; those files are intentionally absent rather than copied
from any other run. Its validation accordingly records
`independent_red_team_performed: false` and `release_ready: false`.
